# План реализации WispWire: статус и очередь этапов

**Дата актуализации:** 28 августа 2026 года

**Спецификация:** `docs/superpowers/specs/2026-08-28-wispwire-tui-design.md`

## Название и границы

Единственное название приложения, Python-пакета, CLI и Homebrew-токена —
**WispWire** / `wispwire`. Этот план не меняет согласованные функциональные
требования и дизайн TUI из спецификации. Он заменяет устаревший план и
отражает только фактический прогресс и порядок дальнейшей работы.

## Правило ведения статуса

- `[x]` ставится только после появления кода, теста, требуемой проверки и
  коммита либо после ссылки на уже существующий подтверждённый коммит.
- `[ ]` означает, что результат ещё отсутствует; наличие текста в старом
  плане не является выполнением.
- Перед началом новой задачи исполнитель сверяет этот документ, целевой
  малый план и `git log`; завершённые этапы не реализуются повторно.
- Каждый новый функциональный этап получает отдельный план в
  `docs/superpowers/plans/`, проходит TDD и завершается фактическими
  проверками.

## Уже реализовано

- [x] Основа пакета `wispwire`, entry point и русская справка CLI — коммит
  `5ab2696`.
- [x] Безопасное обнаружение `tshark`, `dumpcap` и `mergecap` без
  `shell=True` — коммит `121b46b`.
- [x] Модель диагностического отчёта и безопасное получение списка
  интерфейсов — коммит `ab51aeb`.
- [x] Команды `doctor` и `interfaces`, проверка поставляемого entry point и
  устранение ложной доступности live-захвата — коммиты `ed820db`, `943d3e4`,
  `5ed3277`, `1603ffa` и `ebc0382`.
- [x] Актуализированы название WispWire и исходная спецификация — коммит
  `044794e`.

## Очередь оставшейся реализации

### Этап 1: завершить фундамент CLI

- [x] Добавлены команды `wispwire doctor` и `wispwire interfaces` с
  отображением диагностического отчёта.
- [x] Добавлены тесты команд, проверен установленный entry point и обновлён
  README.
- [x] Закрыты задачи 4–5 в
  `2026-08-28-foundation-doctor-interfaces.md` после фактических проверок.

### Этап 2: открыть готовый захват без TUI

- [x] Создать отдельный план `open`: модели сводки пакета и адаптер TShark
  для безопасного потокового чтения PCAP/PCAPNG — коммиты `0c1db54` и
  `be29f40`.
- [x] Добавить `wispwire open PATH` с read-only обработкой исходного файла,
  ограничением количества пакетов и понятными ошибками зависимостей — коммит
  `346c011`.
- [x] Подменять TShark в unit-тестах; реальные PCAP/PCAPNG не добавлять в
  Git — коммит `453cf0c`.

### Этап 3: индекс и поиск

- [x] Создать SQLite-индекс временной сессии, стабильную пагинацию и
  регистронезависимый поиск-подстроку по `Info` — коммиты `81e2f07` и
  `c48013d`.
- [x] Проверить доступность SQLite FTS5 и явно сообщать об её отсутствии —
  коммиты `a30dc5a` и `e849fbf`.

### Этап 4: read-only TUI для файла

- [x] Подключить Textual, таблицу сводок пакетов и текстовые детали
  выбранного кадра.
- [x] Проверить минимальный размер терминала и клавиатурную навигацию.
- [x] Добавить дерево протоколов и hex/ASCII-детали через TShark.

### Этап 5: безопасные временные сессии

- [x] Реализовать manifest, учёт размера, безопасную очистку и удаление
  осиротевших сессий только внутри каталога WispWire.
- [x] Отдельно протестировать выход путей за корень, символьные ссылки и
  безопасное закрытие сессии.

### Этап 6: live-захват

- [x] Реализовать сегментированный захват через внешние Wireshark CLI,
  конечный автомат stop/continue/restart и лимит 1 ГБ (5bf3e7b, 305443f,
  8e7c913, e337d65, b65dec0).
- [x] Добавить snapshot/save через `mergecap`, не меняя исходные файлы
  (0b0c974, e337d65, b65dec0).

### Этап 7: фильтрация и полный TUI

- [x] Добавить display filter TShark и независимый поиск по `Info` для
  файлового TUI с debounce, не меняя переданное пользователем выражение —
  коммиты `bfcf988`, `7a8cc09` и `37ad864`.
- [x] Добавить пакетную доставку событий в UI и согласованную адаптивную
  раскладку live-TUI — коммиты `0284baf`, `7dab395`, `a43eb9a`, `9d5031c`,
  `b5c5748`, `8bde47c`, `8502dd8`, `dd7ac74`, `f1ad361`, `03e552a` и
  `cef41aa`.

### Этап 8: поставка и приёмка

- [ ] Подготовить Homebrew formula/cask, CI, документацию и проверяемые
  smoke-тесты macOS Apple Silicon и Linux ARM64/x86_64.
- [ ] Провести ручную проверку UI и live-захвата: автоматические тесты не
  заменяют реальный захват пакетов.

## Ближайшая исполнимая задача

Следующим реализуется **этап 8 — поставка и приёмка**. Для него нужен отдельный
детальный план поставки, CI и платформенных smoke-тестов; ручная приёмка
live-захвата остаётся самостоятельной проверкой на реальном интерфейсе.
